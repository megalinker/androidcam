# Android / Gradle
android/.gradle/
android/.kotlin/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
*.iml
.DS_Store

# Windows build
windows/build/
windows/out/
windows/*.user
windows/.vs/

# Third-party deps built/downloaded locally (do not commit) — see windows/README.md
windows/third_party/ffmpeg/
windows/third_party/softcam/

# Packaged distributable (large binaries; build with scripts, don't commit)
dist/

# Driver build outputs (WDK)
windows/driver/src/**/x64/
windows/driver/src/**/Win32/
windows/driver/src/**/ARM/
windows/driver/src/**/ARM64/
windows/driver/src/**/*.user

# Keys / signing
*.keystore
*.jks
*.pfx
*.cer

# CMake build trees (never commit build artifacts)
windows/build-webrtc/
windows/build/
